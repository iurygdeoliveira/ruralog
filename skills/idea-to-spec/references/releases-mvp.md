# Deprecado — Releases numeradas (`releases/release-{nn}.md`)

Este arquivo existia para a **Etapa 4** (removida) da skill **idea-to-spec** até a versão **2.x**.

## Estado atual (idea-to-spec v3+)

- **Não** gere pasta `releases/` nem `release-{nn}.md` como parte do pacote.
- **Handoff para SDD / sdd-spec:** `projeto.md` (incluindo **seção 14 — Escopo do MVP e ordem sugerida**), `casos-de-uso/README.md` e os `casos-de-uso/UC-xx-*.md` necessários.
- Priorização por sprint usa **ordem da seção 14**, **MoSCoW** (seção 8), **Matriz** (seção 12) e dependências entre UC/RF — ver skills **sdd-spec** e **sdd-constitution**.

## Legado

Se você encontrar pacotes antigos com `releases/`, trate-os como artefato histórico; prefira migrar o conteúdo de planejamento para a **seção 14** do `projeto.md` e remover duplicação.
